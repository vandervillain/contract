const initialState = {
}

function contractApp(state: any, action: any) {
    if (typeof state === 'undefined') {
        return initialState;
    }

    return state;
}

export default contractApp;